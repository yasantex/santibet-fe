# Base URL of the API. Must be an absolute URL in deployed environments —
# the dev-only proxy in vite.config.ts does not exist in a production build.
VITE_APP_API_BASE_URL=https://santibet-8b7dfea9557d.herokuapp.com

# Google OAuth client ID used by src/utils/googleAuth.ts
VITE_APP_GOOGLE_CLIENT_ID=
